{
  "version_files": [
    {
      "path": "crates/ruminate/Cargo.toml",
      "pattern": "(?ms)^\\[package\\](?:(?!^\\[).)*?^version\\s*=\\s*\"(?P<version>[^\"\\r\\n]+)\"",
      "count": 1
    },
    {
      "path": "Cargo.lock",
      "pattern": "(?m)^name = \"ruminate\"\\r?\\nversion = \"(?P<version>[^\"\\r\\n]+)\"",
      "count": 1
    }
  ],
  "checks": [
    "bash .forgejo/setup-rust.sh",
    "bash .forgejo/check-rust.sh"
  ],
  "build": [
    "bash .forgejo/build-rust.sh"
  ],
  "artifacts": [
    ".release-dist/${RELEASE_NAME}.v${RELEASE_VERSION}-linux-amd64.tar.gz",
    ".release-dist/${RELEASE_NAME}.v${RELEASE_VERSION}-windows-x86_64.zip"
  ],
  "container": {
    "dockerfile": "Dockerfile",
    "context": ".",
    "checks": [
      "bash .forgejo/smoke-container.sh"
    ]
  }
}
